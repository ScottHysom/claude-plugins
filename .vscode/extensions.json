{
  // VS Code offers to install these when the folder is opened.
  // settings.json names the Ruff extension as the Python formatter. The
  // ShellCheck extension shows shell lint problems as you type.
  "recommendations": ["charliermarsh.ruff", "ms-python.python", "timonwong.shellcheck"]
}
