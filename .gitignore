# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editor cruft. .vscode/ is committed on purpose: it carries the repo's
# formatter setup to everyone who clones it. README.md, under "Formatting".
*.swp
*~
.idea/

# Build output
*.plugin
*.zip

# Commit message drafted by Claude, consumed by commit.sh
.commit-msg

# Python: bytecode from importing the plugin scripts, and the test runner's and
# formatter's caches. Contributors also tend to keep a venv here for
# requirements-dev.txt.
__pycache__/
*.py[cod]
.pytest_cache/
.hypothesis/
.ruff_cache/
.venv/
venv/
