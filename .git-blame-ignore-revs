# Commits that only reformat, which `git blame` should look through to find who
# wrote a line. GitHub reads this file on its own; locally, run once per clone:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# A hash here must be the one on main, so add a reformat only after it merges.

# style: reformat the Python with ruff
3953f7af2a58674cae07603def317f71dc00f6c4
