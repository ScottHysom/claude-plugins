# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editor cruft
*.swp
*~
.idea/workspace.xml

# Temp
*.tmp
*.bak

# Commit message drafted by Claude, consumed by commit.sh
.commit-msg

# Files Claude delivers into the chat. Deliverables, not project collateral.
# commit.sh runs `git add -A`, so without this they land in the repo.
Claude outputs/

# prose-tuning copies its script here so Cowork's device can run it. Ignored
# for the same reason as the line above.
.prose-tuning/
